// SPDX-License-Identifier: MIT

pragma solidity ^0.8.25;

contract returnTest {
    /**
     * returns 
     * return
     * 解构赋值  (,_bool2,) = returnNames();
     * 
     * return(1, true, [uint256(1),2,5])  
     * 如果只是1 默认是uint8  所以首个元素需要强转
     */
}